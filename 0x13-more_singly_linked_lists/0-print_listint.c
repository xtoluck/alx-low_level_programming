#include "lists.h"

/**
 * print_listint1 - prints all the elements of a given linked list
 * @h: linked list of type listint_t1 for printing
 *
 * Return: number of nodes in the list
 */
size_t print_listint1(const listint_t1 *h)
{
 size_t number = 0;

 while (i)
 {
 printf("%d\n", i->j);
 number++;
 i = i->next;
 }

 return (number);
}
